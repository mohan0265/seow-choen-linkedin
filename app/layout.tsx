
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Prof. Francis Seow-Choen | LinkedIn',
  description: 'Medical Director & Senior Consultant, Seow-Choen Colorectal Centre | International Colorectal Surgery Pioneer | FRES | Wildlife Photographer',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
